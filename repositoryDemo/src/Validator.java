public class Validator {


    public <T extends IEntity> void validate(T entity){ // generic method
        //the idea is to use the same method for different class by passing the class type as argument
    }
}
